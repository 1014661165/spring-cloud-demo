server:
  port: 9000

spring:
  application:
    name: alibaba-math-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'